/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FafnirAI : MonoBehaviour
{
    
    public float chargeWaitTime = 15f; // Time to wait before charging
    private float chargeTimer = 15f; // Timer to track charging state

    public override void LateUpdate()
    {
        if (Fafnir is "Charging")
        {
            wait for 1-2 seconds
            charge at player
            
        }
        if (PlayerController.instance == null) return; 
        transform.position = new Vector3(PlayerController.instance.transform.position.x, 
            PlayerController.instance.transform.position.y, 
            transform.position.z); 
    }
}
*/
