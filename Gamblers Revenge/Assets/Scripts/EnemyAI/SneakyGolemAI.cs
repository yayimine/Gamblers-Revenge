/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SneakyGolemAI : MonoBehaviour
{

    // Update is called once per frame
    public override void LateUpdate()
    {
        if (Sneaky Golem is in "retreat" )
        {
            if sneaky golem is in frame
            transform.position = new Vector3(-PlayerController.instance.transform.position.x, 
           -PlayerController.instance.transform.position.y, 
           transform.position.z); 
            return;
            if sneaky golem is out of frame
            sneaky golem no longer in retreat
            sneaky golem appears behind the player
        }
        if (PlayerController.instance == null) return; 
        transform.position = new Vector3(PlayerController.instance.transform.position.x, 
            PlayerController.instance.transform.position.y, 
            transform.position.z); 
    }
    
}
*/
